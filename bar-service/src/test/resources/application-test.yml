spring:
  main:
    banner-mode: "off"
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
    consumer:
      group-id: my-consumer
    topic:
      receiver: receiver.t
logging:
  root:
    level: WARN
