spring:
  main:
    banner-mode: "off"
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
    consumer:
      auto-offset-reset: earliest
    topic:
      receiver: receiver.t
logging:
  level:
    root: WARN
